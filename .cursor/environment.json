{
  "name": "AI Team (Chainlit + CrewAI)",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "chainlit",
      "command": ". .venv/bin/activate && PORT=8000 chainlit run aiEmployeeApp.py -h --host 0.0.0.0 --port 8000",
      "description": "Runs the AI Team Chainlit web app on port 8000. Log in with username 'jack' (any password) unless CHAINLIT_PASSWORD is set."
    }
  ],
  "ports": [
    {
      "name": "chainlit web app",
      "port": 8000
    }
  ]
}
